enabled: true            # set to false to silence notifications
channel: "telegram"      # reserved (future: email/webhook)
verbosity: "full"       # brief | full

# NEW
decision_ping: true   # send a short alert when within the EOD window

heartbeat:
  enabled: true
  time: "EOD"   # informational only; we send right after run completes
